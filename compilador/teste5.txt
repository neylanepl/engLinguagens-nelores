void main() {
    int n;
    int m;
    int resultado;

    println("Digite o valor de n: ");
    input("%d", &n);
    println("Digite o valor de m: ");
    input("%d", &m);

    mdc(n, m, &resultado);

    println("O maior divisor comum de " + n + " e " + m + " é: " + resultado);
}

void mdc(int n, int m, int *r) {
    if (m % n == 0) {
        *r = n;f
    } else if (n % m == 0) {
        *r = m;
    } else if (m > n) {
        mdc(n, m % n, r);
    } else {
        mdc(m, n % m, r);
    }
}